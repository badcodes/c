#include <stdio.h>
#include <stdlib.h>
#include <dos.h>
#include "stdset.h"

int main(int argc,char** argv) {
    if(argc!=2) {
        printf("%s v%s, by %s\n",APPNAME,APPVER,APPAUTHOR);
        printf("\t- %s\n",APPDESC);
        printf("Usage:\n\t%s %s",argv[0],APPUSAGE);
        return 1;
    }
}
